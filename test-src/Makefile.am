## Process this file with automake to produce Makefile.in

if RUN_UNIT
  INCLUDE_UNIT = unit
endif

if RUN_FUNCTIONAL
  INCLUDE_FUNCTIONAL = functional
endif

SUBDIRS = $(INCLUDE_UNIT) $(INCLUDE_FUNCTIONAL)